# Technology

No package manifest, source code, build configuration, or dependency lockfile exists yet. The technology stack, libraries, and build/test commands are therefore unknown.

- Inspect the workspace before selecting or adding tooling.
- Follow the conventions established by the first adopted stack and record its build, test, lint, and formatting commands here.
- Do not assume a package manager, runtime, formatter, or test framework.