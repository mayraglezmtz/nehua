# Project Structure

The repository is currently empty except for `.kiro/steering/`, which contains AI-assistant guidance.

- Keep implementation files outside `.kiro/`.
- Organize new code according to the conventions of the chosen framework or build system.
- Add concrete directory responsibilities here once source folders are established.